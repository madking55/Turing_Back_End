<h1>All Songs</h1>

<% @songs.each do |song| %>
    <section id="song-<%= song.id %>">
      <%= render partial: 'info', locals: {song: song } %>
      <%= button_to 'Add Song', "/cart/#{song.id}" , method: :patch %>
    </section>
<% end %> 